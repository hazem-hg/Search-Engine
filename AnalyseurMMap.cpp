#include"utils.h"
#include<map>
/*
AnalyseurMap::AnalyseurMap()
{


}
map<string,int> AnalyseurMap::analyser(vector<string> texte)
{
st.clear();
stat s;
for(vector<string>::iterator i=texte.begin();i!=texte.end();i++)
{
bool exist=false;
for(map<string,int>::iterator j=st.begin();j!=st.end();j++)
{
if(*i==j->first)
{
st[j->first]++;
exist=true;
break;
}
}

if(!exist)
{
//st[*i]=1;;
st.insert(map<string,int>::value_type(*i,1));
}
}

return(st);
}
*/
